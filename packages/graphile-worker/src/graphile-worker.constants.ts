export const GRAPHILE_WORKER_HANDLER_TOKEN = Symbol('GRAPHILE_WORKER_HANDLER');

/**
 * DI token for the Graphile worker module configuration
 */
export const GRAPHILE_WORKER_CONFIG_TOKEN = Symbol(
  'GRAPHILE_WORKER_CONFIG_TOKEN'
);

/**
 * DI token for the Graphile worker utils instance
 */
export const GRAPHILE_WORKER_UTILS_TOKEN = Symbol(
  'GRAPHILE_WORKER_UTILS_TOKEN'
);
